#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @a: function parameter
 * Return: -a or a
 */

int _abs(int)
{
	if (a < 0)
		return (-a);
	else
	{
		return (a);
	}
}
