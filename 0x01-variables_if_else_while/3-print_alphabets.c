#include <stdio.h>
/**
* main -it prints out output of lower case
*
* Return: always (0)
*
*/
int main(void)
{
	int c;

	int d;

	c = 97;
	d = 65;
	while
		(c <=122) {
			putchar(c);
			c++;
		}
	while
		(d <= 90) {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}
